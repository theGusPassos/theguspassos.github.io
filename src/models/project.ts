export default interface Project {
  order: number;
  name: string;
  description: string;
  publicDir: string;
  capsuleAlt: string;
  tags: string[];
  madeWithText: string;
  linkButton: string;
  linkToProject: string;
}
